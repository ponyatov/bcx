#!/usr/bin/env bcx
\ line comment

                \ байт-код начинает выполняться с нулевого адреса:
jmp 0           \ 0 = параметр команды jmp переписывается при каждом
                \ использовании двоеточия : name
                \ при старте байткода запускается последнее слово

: init ( -- )   \ пустое слово инициализации системы (последнее)
;
